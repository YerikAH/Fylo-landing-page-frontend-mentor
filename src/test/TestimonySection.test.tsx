import { render } from '@testing-library/react'
import { TestimonySection } from '../components/main/sections/TestimonySection'
describe('TestimonySection', () => {
  test('test for testimony', () => {
    render(<TestimonySection />)
  })
})
