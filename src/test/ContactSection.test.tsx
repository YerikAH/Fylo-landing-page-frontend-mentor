import { render } from '@testing-library/react'
import ContactSection from '../components/main/sections/ContactSection'
describe('ContactSection', () => {
  test('test for contact', () => {
    render(<ContactSection />)
  })
})
